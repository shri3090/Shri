# VITE_API_URL: Backend API base URL used by the Vite dev proxy.
# In production builds the frontend fetches /api/* relative paths;
# configure your reverse proxy (Nginx, Caddy, etc.) to forward them.
VITE_API_URL="http://localhost:3001"
